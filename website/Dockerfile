# Use the base image from the problem statement
FROM devopsedu/webapp

# Add our website code to the container
ADD . /var/www/html

# The base image likely already has Apache exposed on port 80
# If not, you can add: EXPOSE 80
